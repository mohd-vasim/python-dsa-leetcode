# Recursion and Arrays

## Problem List

- 69. Recursion on List/Arrays
- 70. Check if Array is Sorted
- 71. Sum of an Array
- Coding Exercise 92: Sum of an Array using Recursion
- 72. First Index of an Element in List
- 73. First Index of an Element in List
- Coding Exercise 93: First Index of an Element using Recursion
- 74. Print all Index of An Element
- Coding Exercise 94: Print all Index of Element using Recursion
- 75. Update All Indices in the List provided in Function
- 76. Update Indices in a Global List
- 77. Return List of all Indices

---

## Sum of an Array using Recursion

### Problem Description
You are given an array of integers. Your task is to calculate and return the sum of all elements in the array using recursion.

### Input
- An array of integers.

### Output
- An integer representing the sum of the array elements.

### Examples
- Input: `arr = [1, 2, 3, 4, 5]` → Output: `15`
- Input: `arr = [10, -2, 3, 5]` → Output: `16`

---

## First Index of an Element using Recursion

### Problem Description
You are given an array and a target element. Your task is to find the first index where the array contains that element using recursion. If the element is not found, return `-1`.

### Input
- An array `arr` of integers.
- An integer `element`.

### Output
- An integer representing the first index where `element` is found in `arr`, or `-1` if it is not found.

### Examples
- Input: `arr = [1, 2, 3, 2, 4, 2]`, `element = 2` → Output: `1`
- Input: `arr = [5, 6, 7, 8]`, `element = 10` → Output: `-1`

---

## Print all Index of Element using Recursion

### Problem Description
You are given an array and a target element. Your task is to find all indices where the array contains that element using recursion.

### Input
- An array `arr` of integers.
- An integer `element`.

### Output
- A list of indices where `element` is found in `arr`.

### Examples
- Input: `arr = [1, 2, 3, 2, 4, 2]`, `element = 2` → Output: `[1, 3, 5]`
- Input: `arr = [5, 6, 7, 8]`, `element = 10` → Output: `[]`

---